# Java-CLI-Shell
A simple Command Line Interpreter (CLI) built in Java for an Operating Systems course project at FCAI-CU. This program simulates a basic terminal shell, parsing user input to execute commands like pwd, cd, ls, mkdir, and cp without using the native exec function.
